apiVersion: v1
kind: PersistentVolume
metadata:
  name: clickhouse-pv
spec:
  capacity:
    storage: 6Gi
  accessModes:
    - ReadWriteMany
  storageClassName: standard-rwo
  hostPath:
    path: /data
  persistentVolumeReclaimPolicy: Retain